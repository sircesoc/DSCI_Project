[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_policy_foldflow"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "einops>=0.7.0",
]

[tool.setuptools.packages.find]
where = ["src"]
